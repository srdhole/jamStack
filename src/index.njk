---

 title: Home
 layout: base.njk

---

<div class="container">
        <h1>This is home page</h1>

        <div>
                <ul>
                        {% for article in news.articles %}
                                <li>
                                        <h4>
                                                <a href="article.url">{{article.name}}</a>
                                        </h4>
                                        <p>{{article.author}}</p>
                                </li>
                        {% endfor %}
                </ul>
        </div>
</div>